<p>
  <strong>Date:</strong>
  <%= @job.date %>
</p>

<p>
  <strong>Service hour:</strong>
  <%= @job.service_hour %>
</p>

<p>
  <strong>Total cost:</strong>
  <%= @job.total_cost %>
</p>

<p>
  <strong>Quote:</strong>
  <%= @job.quote_id %>
</p>

<!-- Allow Customer to leave a Review -->
<% if @profile.user_type = "customer" && !(@review.review_to) %>
  <%= link_to "Leave a Review", edit_review_path(@review.id) %>
<% end %>

<%= link_to 'Edit', edit_job_path(@job) %> |
<%= link_to 'Back', jobs_path %>
