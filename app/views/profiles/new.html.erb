<% content_for(:html_title) { 'New Profile' } %>

<%= render 'form', profile: @profile, user_type: @user_type, suburbs: @suburbs, states: @states, postcodes: @postcodes %>

<%= link_to 'Back', profiles_path %>
